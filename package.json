{
  "dependencies": {
    "firebase": "^12.7.0"
  },
  "scripts": {
    "test": "npm --prefix mcp-client test && python -m unittest discover -s scripts/tests -p \"test_*.py\"",
    "test:mcp-client": "npm --prefix mcp-client test",
    "test:scripts": "python -m unittest discover -s scripts/tests -p \"test_*.py\""
  }
}
